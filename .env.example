# Copy to .env and fill in. .env is gitignored and must never be committed.

# AMC vendor API key — sent as the X-API-Key header to api.amctheatres.com
AMC_API_KEY=

# TMDB API read access token
TMDB_API_KEY=
